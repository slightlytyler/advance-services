service: goals

provider:
  name: aws
  runtime: nodejs4.3

# you can overwrite defaults here
#  stage: dev
#  region: us-east-1

# you can define service wide environment variables here
#  environment:
#    variable1: value1

functions:
  fetchCollection:
    handler: handlers.fetchCollection
    events:
      - http:
          path: goals
          method: get
